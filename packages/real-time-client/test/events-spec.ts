describe('Events', () => {
  it.todo(
    'waitForConnection calls back on connect and cleans up after connection established'
  );
  it.todo('onConnect calls back on connection and continues to listen');
  it.todo('onDisconnect calls back on disconnection and continues to listen');
});
