import { expect } from 'chai';
import { FirestoreDb } from './FirestoreDb';
describe('FirestoreDb', function () {
    it('should be defined', function () {
        expect(FirestoreDb).to.exist;
    });
});
//# sourceMappingURL=FirestoreDb.spec.js.map