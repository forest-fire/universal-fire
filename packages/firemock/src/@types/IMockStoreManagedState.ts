/**
 * **IMockStoreManagedState**
 *
 * The _state_ which Firemock's **Store** will be managing.
 *
 * > This interface is internally facing and used by the functions
 * in the `src/databases/store` directory as context so they may
 * provide their functionality.
 */
export interface IMockStoreManagedState {
  //
}
