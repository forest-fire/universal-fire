This directory _decomposes_ the functionality that the **Store** needs into functional areas. Each area will export higher-order functions so that the Store's managed state can be passed in and used
as needed.