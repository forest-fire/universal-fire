import { expect } from 'chai';
import { AbstractedDatabase } from '.';
describe('AbstractedDatabase', () => {
    it('should be defined', () => {
        expect(AbstractedDatabase).to.exist;
    });
});
//# sourceMappingURL=AbstractedDatabase.spec.js.map