{
  "name": "@forest-fire/monorepo",
  "version": "0.1.0",
  "description": "Forest Fire monorepo for Firebase",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/forest-fire/universal-fire.git"
  },
  "scripts": {
    "build": "run-s build:types build:shared build:base build:db build:sdk build:closure",
    "build:shared": "run-p build:shared:utility",
    "build:types": "lerna exec --scope @forest-fire/types yarn build",
    "build:shared:utility": "lerna exec --scope @forest-fire/utility yarn build",
    "build:base": "lerna exec --scope @forest-fire/abstracted-database yarn build",
    "build:db": "run-p build:db:rtdb build:db:firestore",
    "build:db:rtdb": "lerna exec --scope @forest-fire/real-time-db yarn build",
    "build:db:firestore": "lerna exec --scope @forest-fire/firestore-db yarn build",
    "build:sdk": "run-p build:sdk:rtdb build:sdk:firestore:client ",
    "build:sdk:rtdb": "run-p build:sdk:rtdb:client build:sdk:rtdb:admin",
    "build:sdk:rtdb:client": "lerna exec --scope @forest-fire/real-time-client yarn build",
    "build:sdk:rtdb:admin": "lerna exec --scope @forest-fire/real-time-admin yarn build",
    "build:sdk:firestore:client": "lerna exec --scope @forest-fire/firestore-client yarn build",
    "build:closure": "lerna exec --scope universal-fire yarn build",
    "watch": "run-p watch:rtdb watch:firestore",
    "watch:rtdb": "run-p 'yarn lerna exec --scope @forest-fire/real-time-db yarn watch' 'yarn lerna exec --scope @forest-fire/real-time-client yarn watch' 'yarn lerna exec --scope @forest-fire/real-time-admin yarn watch'",
    "watch:firestore": "run-p 'yarn lerna exec --scope @forest-fire/firestore-db yarn watch' 'yarn lerna exec --scope @forest-fire/firestore-client yarn watch' 'yarn lerna exec --scope @forest-fire/firestore-admin yarn watch'",
    "docs": "yarn lerna exec --scope @forest-fire/docs yarn docs",
    "docs:build": "yarn lerna exec --scope @forest-fire/docs yarn docs:build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "chai": "^4.2.0",
    "do-devops": "^0.3.6",
    "lerna": "^3.20.2",
    "mocha": "^7.1.1",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.0.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.9.1",
    "typescript": "^3.8.3"
  },
  "license": "MIT"
}